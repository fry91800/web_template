const nextConfig = {
  i18n: {
    locales: ['en', 'fr'], // Define the supported languages
    defaultLocale: 'en',  // Default language
  },
  defaultNS: 'common', // Set the default namespace to 'common'
};

export default nextConfig;
